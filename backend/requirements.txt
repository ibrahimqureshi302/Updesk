# ── Django Core ───────────────────────────────────────────────────────────────
Django==4.2.13
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-filter==23.5

# ── Database ──────────────────────────────────────────────────────────────────
psycopg[binary]==3.3.4

# ── Celery + Redis (background sync jobs) ────────────────────────────────────
celery==5.3.6
redis==5.0.4
django-celery-beat==2.6.0
django-celery-results==2.5.1

# ── Upwork + HTTP ─────────────────────────────────────────────────────────────
python-upwork==2.1.0
requests==2.31.0
requests-oauthlib==1.3.1

# ── JWT (React <-> Django auth) ───────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Encryption (token storage) ────────────────────────────────────────────────
cryptography==42.0.8

# ── Config / Env ──────────────────────────────────────────────────────────────
python-decouple==3.8

# ── Retry on API failures ─────────────────────────────────────────────────────
tenacity==8.2.3

# ── Dev utilities ─────────────────────────────────────────────────────────────
django-extensions==3.2.3

#── Swagger ─────────────────────────────────────────────────────────────
drf-spectacular==0.27.1

# ── Production server + static files (Docker) ────────────────────────────────
gunicorn==21.2.0
whitenoise==6.6.0

# ── WhatsApp notifications use Evolution API (self-hosted) via requests ──────
# No extra package needed — uses requests which is already listed above.


